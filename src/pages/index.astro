---
import Layout from '~/layouts/Layout.astro';

const metadata = {
  title: "Purple Martin — There's a new bird in town.",
  ignoreTitleTemplate: true,
};
---

<Layout metadata={metadata}> Purple Martin Index Page </Layout>
