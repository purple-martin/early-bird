---
import Layout from '~/layouts/Layout.astro';
import '~/assets/styles/Theme.css';
import '~/assets/styles/diagonals.css';

const metadata = {
  title: "Purple Martin — There's a new bird in town.",
  ignoreTitleTemplate: true,
};
---

<Layout metadata={metadata}> Purple Martin </Layout>
